#include <bits/stdc++.h>
using namespace std;

bool checkRotations(string s, string goal)
{
    string new_s = s + s; // it contains all rotations of s

    // to find goal in string "new_s"
    // KMP alogrithm
}
int main()
{
    return 0;
}