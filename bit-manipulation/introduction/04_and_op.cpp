#include <bits/stdc++.h>
using namespace std;

int main() {
    int num = 9;
    num = num ^ 7; // XOR
    cout<<num;

    cout<<"Shift operators....\n";
    cout<<"Right shift: "<<(13 >> 1)<<"\n";
    cout<<"Left shift: "<<(13 << 1)<<"\n";
    cout<<"Power: "<<(1 << 3);
}